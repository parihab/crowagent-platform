# .env.example - sample environment variables
# COPY THIS FILE TO `.env` OR TRANSLATE THE VALUES INTO
# `.streamlit/secrets.toml` before running the app.
# DO NOT commit real API keys to version control.

# Google Gemini / Vertex AI key (chat, embeddings, etc.)
GEMINI_KEY=AIzaSy...   # replace with your own

# Optional Met Office DataPoint key (weather service)
MET_OFFICE_KEY=abcdef1234567890
