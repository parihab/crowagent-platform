# .env.example — copy to `.env` and fill in your own values.
# NEVER commit the real `.env` to version control.
# Keys can also be placed in `.streamlit/secrets.toml` (preferred for Streamlit Cloud).
# See: https://docs.streamlit.io/deploy/streamlit-community-cloud/deploy-your-app/secrets-management

# ── EPC Open Data Communities ────────────────────────────────────────────────
# Request free credentials at:
#   https://epc.opendatacommunities.org/docs/guidance#FAQ
EPC_API_KEY=your-epc-api-key-here
# Optional: override the registered e-mail used as the Basic-Auth username.
# Defaults to the account registered with the EPC Open Data Communities portal.
# EPC_USERNAME=your-registered-email@example.com

# ── EPC API base URL (optional, defaults shown) ──────────────────────────────
# EPC_API_URL=https://epc.opendatacommunities.org/api/v1

# ── Google Gemini / AI Advisor ───────────────────────────────────────────────
# Get a free key at https://aistudio.google.com
GEMINI_KEY=AIzaSy...   # replace with your own key (starts with AIzaSy)

# ── Met Office DataPoint (optional weather provider) ─────────────────────────
# Register at https://www.metoffice.gov.uk/services/data/datapoint
# MET_OFFICE_KEY=your-met-office-key-here
